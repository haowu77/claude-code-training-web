# Claude Code 文件位置指南

> 配置文件位置速查

---

## 📁 记忆文件 (CLAUDE.md)

| 位置 | 范围 | 共享 | 用途 |
|------|------|------|------|
| `~/.claude/CLAUDE.md` | 全局 | ❌ 仅本人 | 个人偏好、跨项目设置 |
| `./CLAUDE.md` | 项目 | ✅ Git 共享 | 项目标准、团队约定 |
| `./.claude/CLAUDE.md` | 项目 | ✅ Git 共享 | 同上（备选位置） |

**优先级**: 项目 > 用户

---

## ⚙️ 配置文件 (settings.json)

| 位置 | 范围 | 共享 | 用途 |
|------|------|------|------|
| `~/.claude/settings.json` | 全局 | ❌ 仅本人 | 个人配置、默认设置 |
| `.claude/settings.json` | 项目 | ✅ Git 共享 | 项目配置、团队设置 |
| `.claude/settings.local.json` | 项目 | ❌ 本地 | 本地覆盖、私有设置 |

**优先级**: 本地 > 项目 > 用户

---

## 🔌 MCP 配置

| 位置 | 范围 | 包含令牌 | 共享 |
|------|------|----------|------|
| `~/.claude/.mcp.json` | 全局 | ✅ | ❌ 仅本人 |
| `.mcp.json` | 项目 | ❌ | ✅ Git 共享 |
| `.claude/mcp.json` | 项目 | ✅ | ❌ 本地 |

**提示**:
- `.mcp.json` (无令牌) → 提交到 Git
- `.claude/mcp.json` (有令牌) → 添加到 .gitignore

---

## 🤖 自定义 Agent

| 位置 | 范围 | 共享 |
|------|------|------|
| `~/.claude/agents/` | 全局 | ❌ 仅本人 |
| `.claude/agents/` | 项目 | ✅ Git 共享 |

**示例**:
```
.claude/agents/
├── code-reviewer.md
├── test-writer.md
└── security-auditor.md
```

---

## 📝 自定义命令

| 位置 | 范围 | 共享 |
|------|------|------|
| `~/.claude/commands/` | 全局 | ❌ 仅本人 |
| `.claude/commands/` | 项目 | ✅ Git 共享 |

**示例**:
```
.claude/commands/
├── optimize.md
├── test-all.md
└── deploy.md
```

---

## 📦 会话和缓存

| 位置 | 用途 | 共享 |
|------|------|------|
| `.claude/sessions/` | 会话历史 | ❌ 不提交 |
| `.claude/checkpoints/` | 会话检查点 | ❌ 不提交 |
| `.claude/cache/` | 临时缓存 | ❌ 不提交 |
| `.claude/audit.log` | 审计日志 | ❌ 不提交 |

**提示**: 全部添加到 `.gitignore`

---

## 🏢 企业策略配置

| 系统 | 位置 |
|------|------|
| macOS/Linux | `/etc/claude-code/` |
| Windows | `C:\ProgramData\Claude Code\` |

**特点**:
- 最高优先级
- 不可被用户覆盖
- 由 MDM/Group Policy 管理

---

## 📋 完整目录结构

### 项目级别
```
your-project/
├── CLAUDE.md                    # ✅ 提交
├── .mcp.json                    # ✅ 提交（无令牌）
├── .claude/
│   ├── CLAUDE.md                # ✅ 提交（备选）
│   ├── settings.json            # ✅ 提交
│   ├── settings.local.json      # ❌ 不提交
│   ├── mcp.json                 # ❌ 不提交（有令牌）
│   ├── agents/                  # ✅ 提交
│   │   ├── code-reviewer.md
│   │   └── test-writer.md
│   ├── commands/                # ✅ 提交
│   │   ├── optimize.md
│   │   └── deploy.md
│   ├── sessions/                # ❌ 不提交
│   ├── checkpoints/             # ❌ 不提交
│   └── audit.log                # ❌ 不提交
└── .gitignore
```

### 用户级别
```
~/.claude/
├── CLAUDE.md
├── settings.json
├── .mcp.json
├── agents/
│   ├── my-python-expert.md
│   └── my-react-specialist.md
├── commands/
│   ├── my-workflow.md
│   └── my-utils.md
└── sessions/
```

---

## 🔒 .gitignore 推荐

```gitignore
# Claude Code 本地文件
.claude/mcp.json
.claude/settings.local.json
.claude/sessions/
.claude/checkpoints/
.claude/cache/
.claude/audit.log
.claude/*.log

# 但保留这些（如果存在）
!.claude/settings.json
!.claude/agents/
!.claude/commands/
```

---

## 💡 配置优先级

**记忆 (CLAUDE.md)**:
```
企业策略 > 项目 CLAUDE.md > 用户 CLAUDE.md
```

**设置 (settings.json)**:
```
企业策略 > 命令行参数 > 本地设置 > 项目设置 > 用户设置
```

**MCP 配置**:
```
企业策略 > 本地 MCP > 项目 MCP > 用户 MCP
```

---

## 🎯 快速访问

### 编辑记忆
```bash
/memory                  # 在 Claude Code 中编辑
# 或
nano ./CLAUDE.md         # 直接编辑项目记忆
nano ~/.claude/CLAUDE.md # 直接编辑用户记忆
```

### 编辑配置
```bash
/config                  # 交互式配置
# 或
nano .claude/settings.json
```

### 管理 Agents
```bash
/agents                  # 在 Claude Code 中管理
# 或
ls .claude/agents/       # 查看项目 Agents
ls ~/.claude/agents/     # 查看用户 Agents
```

### 管理命令
```bash
ls .claude/commands/     # 查看项目命令
ls ~/.claude/commands/   # 查看用户命令
```

---

**提示**: 使用 `code .claude/` 在 VS Code 中打开整个配置目录！
